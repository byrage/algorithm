"""
# " "로 분리된 input을 각각 받기
X = input()
A, B = map(int, X.split(" "))

# " "로 분리된 input을 array로 받기
scale = list(map(int, input().split()))

# iteration 횟수만 지정
for i in range(count):

# 배열 생성 및 값 추가
scores = []
scores.append(score)
"""